# Server Port
PORT=3000

# MongoDB Connection String (Replace with your Atlas URI in production)
MONGODB_URI=mongodb://127.0.0.1:27017/mind_maze_books

# Secret key for JWT signing (Use a strong random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_later

# Environment
NODE_ENV=development
